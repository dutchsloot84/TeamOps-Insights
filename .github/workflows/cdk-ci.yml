name: cdk-ci

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
    paths:
      - 'infra/**'
      - 'cdk/**'
      - 'cdk.json'
      - 'package*.json'
      - 'requirements*.txt'
      - '.github/workflows/cdk-ci.yml'
  workflow_dispatch:

jobs:
  validate-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-west-2
      NPX_YES: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (20.x)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python (3.11)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install CDK & Python deps
        run: |
          set -euo pipefail
          python -m venv .venv
          source .venv/bin/activate
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install -r infra/cdk/requirements.txt
          npm install -g aws-cdk

      - name: Diagnose working dir & cdk.json
        run: |
          set -xeuo pipefail
          echo "PWD=$(pwd)"
          ls -la
          echo '--- cdk.json ---'
          test -f cdk.json && cat cdk.json || echo 'NO ROOT cdk.json FOUND'

      - name: CDK List
        run: |
          set -euo pipefail
          source .venv/bin/activate
          npx cdk list -a "python -m infra.cdk.app"

      - name: CDK Synth
        run: |
          set -euo pipefail
          source .venv/bin/activate
          npx cdk synth -a "python -m infra.cdk.app"

      - name: Configure AWS credentials
        if: vars.OIDC_ROLE_ARN != ''
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
          role-session-name: cdk-deploy-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}

      - name: CDK Diff
        if: vars.OIDC_ROLE_ARN != ''
        run: |
          set -euo pipefail
          source .venv/bin/activate
          npx cdk diff -a "python -m infra.cdk.app"

      - name: CDK Deploy
        if: >-
          vars.OIDC_ROLE_ARN != '' &&
          github.event_name == 'push' &&
          (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
        run: |
          set -euo pipefail
          source .venv/bin/activate
          npx cdk deploy -a "python -m infra.cdk.app" --require-approval never